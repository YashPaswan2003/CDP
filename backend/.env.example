# Database
DB_PATH=./ethinos.duckdb

# Security
SECRET_KEY=your-secret-key-here-change-in-production

# CORS Origins (comma-separated list)
# Frontend development servers + production
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,https://ethinos-cdp.pages.dev,https://main.ethinos-cdp.pages.dev

# Debug mode (set to false in production)
DEBUG=True
